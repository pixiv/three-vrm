{
  "name": "@pixiv/three-vrm-constraints",
  "version": "0.0.1",
  "description": "Constraints module for @pixiv/three-vrm",
  "license": "MIT",
  "author": "pixiv",
  "files": [
    "/lib/",
    "/ts*/",
    "/types/",
    "LICENSE"
  ],
  "main": "lib/three-vrm-constraints.min.js",
  "types": "types/index.d.ts",
  "typesVersions": {
    "<3.9": {
      "*": [
        "ts3.4/*"
      ]
    }
  },
  "scripts": {
    "version": "yarn all",
    "all": "yarn && yarn lint && yarn clean && yarn build && yarn docs",
    "dev": "cross-env NODE_ENV=development SERVE=1 rollup -w -c -o lib/three-vrm-constraints.js",
    "clean": "rimraf docs/ lib/ ts*/ types/",
    "build": "yarn build-dev && yarn build-prod && yarn build-types",
    "build-dev": "cross-env NODE_ENV=development rollup -c -o lib/three-vrm-constraints.js",
    "build-prod": "cross-env NODE_ENV=production rollup -c -o lib/three-vrm-constraints.min.js",
    "build-types": "tsc --declaration --declarationDir ./types --emitDeclarationOnly && downlevel-dts types ts3.4/types",
    "docs": "typedoc --out docs --mode file --excludeNotExported",
    "test": "jest",
    "lint": "eslint \"src/**/*.{ts,tsx}\" && prettier \"src/**/*.{ts,tsx}\" --check",
    "lint-fix": "eslint \"src/**/*.{ts,tsx}\" --fix && prettier \"src/**/*.{ts,tsx}\" --write"
  },
  "lint-staged": {
    "./src/**/*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ]
  },
  "dependencies": {
    "@pixiv/types-vrmc-constraints-1.0": "0.0.1"
  },
  "devDependencies": {
    "downlevel-dts": "^0.5.0",
    "three": "^0.117.1",
    "typedoc": "^0.17.7"
  },
  "peerDependencies": {
    "three": "^0.117.1"
  }
}
